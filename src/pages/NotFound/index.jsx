//Este componente es la página de Not Found, cuando ingresan una ruta que no existe
//Components
import { Layout } from "../../Components/Layout"

function NotFound() {

    return (
      <Layout>
        NotFound
      </Layout>
    )
  }
  
export { NotFound }